---
description: VGGalgameCore 头文件依赖边界（Phase 7）
globs: "Engine/Source/Runtime/VGGalgameCore/**/*"
alwaysApply: false
---

# VGGalgameCore 依赖规则

在 `Engine/Source/Runtime/VGGalgameCore/` 内新增或修改代码时遵守：

1. **禁止** `#include` 以下路径前缀（含子路径）：
   - `VGGalgameNodeGraph/`
   - `VGEditor` / `Editor/` 下任意编辑器模块

   **例外（Phase 8）**：`VGGalgameCore/` 现为 **薄转发头** 目录，可 `#include "VGGalgameSequenceRuntime/..."` 等以指向真实模块；禁止在转发头之外手写业务逻辑。

2. **允许**：标准库、`VGGalgameCore` 自身、`VGCore`、`VGEngine`、`VGRHI`、`HCore`、`VGLua`/`sol` 等与 Core CMake `target_link_libraries` 一致的公共依赖。

3. **`VGGalgameCore/Source`** 不新增具体玩法逻辑；仅数据容器、序列化、Guard、引擎访问辅助等基础设施例外，且须在 PR 中说明。

4. 若必须跨层协作，将契约放在 Core `Interface/`，实现在 `VGGalgame` 或 `VGGalgameNodeGraph`（纯节点执行与数据，不含剧情宿主逻辑）。
