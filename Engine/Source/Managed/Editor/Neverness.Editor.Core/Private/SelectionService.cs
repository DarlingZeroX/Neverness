namespace Neverness.Editor.Core.Private;

/// <summary>
/// 编辑器全局选取状态（当前选中的 <see cref="NNEntityHandle"/>）。
/// </summary>
public sealed class SelectionService
{
	/// <summary>目前选取实体句柄；null 或零值表示无选取。</summary>
	//public NNEntityHandle? SelectedEntity { get; private set; }

	/// <summary>设定选取。</summary>
	//public void SetSelected(NNEntityHandle? handle) => SelectedEntity = handle;

	/// <summary>清除选取。</summary>
	//public void Clear() => SelectedEntity = null;

	/// <summary>是否有有效选取。</summary>
	//public bool HasSelection => SelectedEntity is { Value: not 0 };
}
