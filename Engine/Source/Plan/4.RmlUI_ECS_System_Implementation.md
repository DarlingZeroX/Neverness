# RmlUI ECS 系统实现计划（v4 · 最终版）

> 日期：2026-05-30
> 模块：NNRuntimeRmlui / NNRuntimeRenderer2D / NNRuntimeScene
> 状态：Step 1-2 可实施，Step 3-6 待修正后实施
> 基于用户第三轮架构审查反馈修订

---

## 架构定稿

```
┌─────────────────────────────────────────────────────────┐
│  NNRuntimeScene                                          │
│  ├─ NNRmlUIDocumentComponent (纯数据)                    │
│  ├─ IAssetResolver* (注入，不拥有)                        │
│  └─ NNRmlUISystem : ISceneSystem                        │
│       └─ Tick → 构建 RmlDrawList (纯数据，无 rendererHandle)│
└─────────────────────────────────────────────────────────┘
              ↓ RmlDrawList (只读)

┌─────────────────────────────────────────────────────────┐
│  SceneRenderer                                           │
│  ├─ SpriteRenderer → SpriteDrawCommands                  │
│  └─ RmlUIRenderer (持有 Rml::Context)                    │
│       ├─ SyncDrawList(drawList)                          │
│       │    ├─ 新 entityId → GetOrCreateDocument(guid)    │
│       │    ├─ 不在 list → HideDocument                   │
│       │    └─ 已销毁 → UnloadDocument                    │
│       ├─ Update()                                        │
│       └─ Render(viewMode)                                │
└─────────────────────────────────────────────────────────┘
```

---

## 三层职责边界（硬性约束）

| 层 | 持有 | 不持有 |
|----|------|--------|
| **NNRuntimeScene** | NNRmlUIDocumentComponent 数据 | Rml 任何东西 |
| **NNRmlUISystem** | EntityId → 组件状态映射 | Rml::ElementDocument / Context |
| **RmlUIRenderer** | Rml::Context + ElementDocument 实例 | ECS 组件 / PlayMode 状态 |

---

## Step 1: IAssetResolver + NNRmlUIDocumentComponent 修订

**可以立即实施。**

### 1a: IAssetResolver 接口

**新增文件**: `NNRuntimeScene/Include/Assets/IAssetResolver.h`

```cpp
class IAssetResolver
{
public:
    virtual ~IAssetResolver() = default;
    virtual bool Resolve(NNGuid guid, char* outPath, uint32_t outPathSize) = 0;
};
```

### 1b: NNRuntimeScene 注入

**修改文件**: `NNRuntimeScene/Include/Scene/NNRuntimeScene.h`

```cpp
class NNRuntimeScene
{
public:
    void SetAssetResolver(IAssetResolver* resolver);
    IAssetResolver* GetAssetResolver() const;
private:
    IAssetResolver* m_AssetResolver = nullptr;
};
```

### 1c: NNRmlUIDocumentComponent 修订

**修改文件**: `NNRuntimeScene/Include/Components/NNRmlUIDocumentComponent.h`

```cpp
enum class NNRmlUIViewTarget : std::uint32_t
{
    Scene = 0,
    Game  = 1,
    Both  = 2,
};

enum class NNRmlUIDocumentFlags : std::uint32_t
{
    None          = 0,
    AutoLoad      = 1u << 0,
    Focusable     = 1u << 1,
    ReceivesInput = 1u << 2,
};

struct NNRmlUIDocumentComponent
{
    NNGuid                DocumentAsset{};
    NNRmlUIDocumentFlags  Flags = NNRmlUIDocumentFlags::AutoLoad
                                | NNRmlUIDocumentFlags::ReceivesInput;
    std::int32_t          SortOrder = 0;
    NNRmlUIViewTarget     ViewTarget = NNRmlUIViewTarget::Both;
};
// 28 字节
```

- [x] 创建 IAssetResolver.h
- [x] NNRuntimeScene 新增 AssetResolver 注入
- [x] 修改 NNRmlUIDocumentComponent（移除 ResolvedPath，新增 Visibility）
- [x] 更新 BuiltinComponentRegistration
- [x] 更新 C# NNRmlUIDocumentComponentData + NNRmlUIVisibility enum

---

## Step 2: NNRmlUISystem（纯数据输出）

**可以立即实施。**

**新增文件**: `NNRuntimeRmlui/Include/System/NNRmlUISystem.h`

```cpp
/// @brief RmlUI 绘制项——纯数据，无 Rml SDK 依赖。
struct RmlDrawItem
{
    NNEntity entity{};          // 带 generation 的实体句柄（防复用）
    NNGuid assetGuid{};
    int32_t sortOrder = 0;
    NNRmlUIViewTarget viewTarget = NNRmlUIViewTarget::Both;
};

/// @brief RmlUI ECS 系统——只负责构建 DrawList。
class NNRmlUISystem : public ISceneSystem
{
public:
    NNSceneTickGroup TickGroup() const noexcept override;  // EarlyUpdate
    void Tick(NNRuntimeScene& scene, float dt) noexcept override;

    const std::vector<RmlDrawItem>& GetDrawList() const;

private:
    void SyncDocuments(NNRuntimeScene& scene);

    // EntityId → 组件快照（用于 diff）
    struct EntrySnapshot
    {
        NNGuid assetGuid{};
        int32_t sortOrder = 0;
        NNRmlUIViewTarget viewTarget = NNRmlUIViewTarget::Both;
        NNRmlUIDocumentFlags flags{};
        bool alive = false;
    };

    std::unordered_map<uint32_t, EntrySnapshot> m_Snapshots;
    std::vector<RmlDrawItem> m_DrawList;
    bool m_DrawListDirty = true;
};
```

**Tick 实现**：

```cpp
void NNRmlUISystem::Tick(NNRuntimeScene& scene, float dt) noexcept
{
    SyncDocuments(scene);
}

void NNRmlUISystem::SyncDocuments(NNRuntimeScene& scene)
{
    // 1. 标记所有快照为 not alive
    for (auto& [id, snap] : m_Snapshots)
        snap.alive = false;

    // 2. 扫描 ECS，更新快照
    scene.Query<NNRmlUIDocumentComponent>().Each(
        [&](NNEntity entity, NNRmlUIDocumentComponent& comp)
        {
            uint32_t id = entity.Value;
            auto& snap = m_Snapshots[id];
            bool changed = (snap.assetGuid != comp.DocumentAsset ||
                           snap.sortOrder != comp.SortOrder ||
                           snap.viewTarget != comp.ViewTarget ||
                           snap.flags != comp.Flags);
            if (changed) m_DrawListDirty = true;

            snap.assetGuid = comp.DocumentAsset;
            snap.sortOrder = comp.SortOrder;
            snap.viewTarget = comp.ViewTarget;
            snap.flags = comp.Flags;
            snap.alive = true;
        });

    // 3. 移除已销毁实体
    for (auto it = m_Snapshots.begin(); it != m_Snapshots.end(); )
    {
        if (!it->second.alive)
        {
            m_DrawListDirty = true;
            it = m_Snapshots.erase(it);
        }
        else
            ++it;
    }

    // 4. 重建 DrawList（仅在脏时）
    if (m_DrawListDirty)
    {
        m_DrawList.clear();
        for (const auto& [id, snap] : m_Snapshots)
        {
            if (snap.assetGuid.High == 0 && snap.assetGuid.Low == 0)
                continue;

            m_DrawList.push_back({
                .entityId = id,
                .assetGuid = snap.assetGuid,
                .sortOrder = snap.sortOrder,
                .viewTarget = snap.viewTarget,
                .enabled = HasFlag(snap.flags, NNRmlUIDocumentFlags::AutoLoad),
            });
        }

        // 按 SortOrder 排序
        std::sort(m_DrawList.begin(), m_DrawList.end(),
            [](const RmlDrawItem& a, const RmlDrawItem& b)
            { return a.sortOrder < b.sortOrder; });

        m_DrawListDirty = false;
    }
}
```

- [x] 创建 NNRmlUISystem 头文件
- [x] 实现 Tick（SyncDocuments + 构建 DrawList）
- [x] 确保无 Rml SDK 依赖

---

## Step 3: RmlUIRenderer（v4 修正版）

**⚠️ 关键修正（基于架构审查反馈）：**
1. **Sync() 从 Render() 中拆出** — 文档加载/卸载在 Update 阶段，Render 纯渲染
2. **EntityKey 用 NNEntity（带 generation）** — 防止 Entity 复用导致旧 Document 绑定新 Entity
3. **移除 Loading 状态** — LoadDocument 是同步的，Loading 只存在几微秒，无意义
4. **Visibility → ViewTarget** — 避免与 Visible/Hidden 混淆
5. **AutoLoad 只影响初始加载** — 运行时启用/禁用用单独的 Enabled 状态

**新增文件**: `NNRuntimeRenderer2D/Include/Renderer2D/RmlUIRenderer.h`

```cpp
/// @brief 文档运行时状态（简化，无 Loading）。
enum class RmlDocState : uint8_t
{
    Ready,      // 已加载，可渲染
    Hidden,     // 已加载但不可见
    Failed,     // 加载失败
};

/// @brief 文档运行时实例（Renderer 内部管理）。
struct RmlDocumentRuntime
{
    Rml::ElementDocument* doc = nullptr;
    RmlDocState state = RmlDocState::Ready;
    NNGuid assetGuid{};
};

/// @brief RmlUI 渲染器——持有 Context，管理文档实例。
///
/// 生命周期分离：
/// - Sync()  — 在 Update 阶段调用，同步 DrawList → 加载/卸载文档
/// - Update() — 更新 Context（Layout + 动画）
/// - Render() — 纯渲染，不加载/卸载任何资源
class RmlUIRenderer
{
public:
    bool Initialize(uint32_t viewportWidth, uint32_t viewportHeight);
    void Shutdown();
    void SetViewport(uint32_t width, uint32_t height);

    /// @brief 同步 DrawList（Update 阶段调用）。
    /// 加载新文档、卸载不再需要的文档。
    /// 不在 Render 阶段调用。
    void Sync(const std::vector<RmlDrawItem>& drawList);

    /// @brief 更新 Context（Update 阶段调用）。
    void Update();

    /// @brief 纯渲染（Render 阶段调用）。
    /// 不加载/卸载任何资源。
    void Render(const std::vector<RmlDrawItem>& drawList,
                NNRmlUIViewTarget viewTarget);

    void ProcessInput(const NNRmlUIInputEvent& event);
    void SetAssetResolver(IAssetResolver* resolver);

private:
    Rml::ElementDocument* LoadDocument(NNGuid assetGuid);
    void UnloadDocument(NNEntity entity);

    Rml::Context* m_Context = nullptr;
    RenderInterface_GL3* m_RenderInterface = nullptr;
    SystemInterface_SDL* m_SystemInterface = nullptr;
    UIFileInterfaceVFS* m_FileInterface = nullptr;
    IAssetResolver* m_AssetResolver = nullptr;

    // NNEntity（带 generation）→ 文档运行时实例
    // 防止 Entity 复用导致旧 Document 绑定新 Entity
    std::unordered_map<NNEntity, RmlDocumentRuntime,
                       NNEntityHash> m_Documents;

    uint32_t m_ViewportWidth = 0;
    uint32_t m_ViewportHeight = 0;
    bool m_Initialized = false;
};
```

**Sync 实现（Update 阶段，不在 Render 中）**：

```cpp
void RmlUIRenderer::Sync(const std::vector<RmlDrawItem>& drawList)
{
    // 收集 active entity 集合
    std::unordered_set<NNEntity, NNEntityHash> activeEntities;
    for (const auto& item : drawList)
        activeEntities.insert(item.entity);

    // 卸载不在 list 中的文档
    for (auto it = m_Documents.begin(); it != m_Documents.end(); )
    {
        if (!activeEntities.contains(it->first))
        {
            UnloadDocument(it->first);
            it = m_Documents.erase(it);
        }
        else
            ++it;
    }

    // 加载新文档
    for (const auto& item : drawList)
    {
        if (m_Documents.contains(item.entity))
            continue;

        auto* doc = LoadDocument(item.assetGuid);
        if (doc)
        {
            doc->Show();
            m_Documents[item.entity] = { doc, RmlDocState::Ready, item.assetGuid };
        }
        else
        {
            m_Documents[item.entity] = { nullptr, RmlDocState::Failed, item.assetGuid };
        }
    }
}

Rml::ElementDocument* RmlUIRenderer::LoadDocument(NNGuid assetGuid)
{
    char path[512];
    if (!m_AssetResolver || !m_AssetResolver->Resolve(assetGuid, path, sizeof(path)))
        return nullptr;
    return m_Context->LoadDocument(path);
}

void RmlUIRenderer::UnloadDocument(NNEntity entity)
{
    auto it = m_Documents.find(entity);
    if (it != m_Documents.end() && it->second.doc)
        it->second.doc->Close();
}
```

**Update 实现**：

```cpp
void RmlUIRenderer::Update()
{
    if (!m_Initialized) return;
    m_Context->Update();
}
```

**Render 实现（纯渲染，不加载/卸载）**：

```cpp
void RmlUIRenderer::Render(const std::vector<RmlDrawItem>& drawList,
                           NNRmlUIViewTarget viewTarget)
{
    if (!m_Initialized) return;

    m_RenderInterface->BeginFrame();

    for (const auto& item : drawList)
    {
        // ViewTarget 过滤
        if (item.viewTarget != NNRmlUIViewTarget::Both &&
            item.viewTarget != viewTarget)
            continue;

        auto it = m_Documents.find(item.entity);
        if (it != m_Documents.end() && it->second.state == RmlDocState::Ready)
        {
            it->second.doc->UpdateDocument();
            it->second.doc->Render();
        }
    }

    m_RenderInterface->EndFrame();
}
```

- [x] 创建 RmlUIRenderer（放在 NNRuntimeRmlui 模块，避免跨模块依赖）
- [x] 实现 RmlDocState 状态机（Ready / Hidden / Failed）
- [x] 实现 Sync（Update 阶段，加载/卸载文档）
- [x] 实现 Update（Context->Update）
- [x] 实现 Render（纯渲染，ViewTarget 过滤）
- [x] 实现 ProcessInput

---

## Step 4: ViewportRenderRuntimeApi 编排

**依赖 Step 3 完成后实施。**

RmlUIRenderer 在 NNRuntimeRmlui 模块，SceneRenderer 在 NNRuntimeRenderer2D 模块。
为避免跨模块依赖，由 ViewportRenderRuntimeApi 编排两者的调用顺序。

- [x] ViewportRenderRuntimeApi 持有 RmlUIRenderer*
- [x] RenderSceneToTexture 中：先 SceneRenderer::Render()，再 RmlUIRenderer::Sync() + Update() + Render()
- [x] 不修改 SceneRenderer（保持职责单一）

---

## Step 5: 编译验证 + CMakeLists 更新

**依赖 Step 4 完成后实施。**

- [ ] 更新 NNRuntimeRmlui CMakeLists 添加新文件
- [ ] 更新 NNRuntimeEngineServices CMakeLists 添加 RmlUI 依赖
- [ ] 编译通过

---

## Step 6: 输入处理 + Editor 侧实现

**依赖 Step 5 完成后实施。**

- [x] 定义 NNRmlUIInputEvent 结构体（直接在 ViewportRenderAPI 中定义函数签名）
- [x] ViewportRenderAPI 新增函数指针（SetRmlUIViewportSize + ProcessRmlUIInput）
- [ ] Editor 侧 IAssetResolver 实现（待 C# 编译验证后实施）
- [ ] Editor 侧输入条件转发（待 C# 编译验证后实施）

---

## 实施阶段划分

| 阶段 | Steps | 状态 | 说明 |
|------|-------|------|------|
| **Phase 1** | Step 1 + 2 | ✅ 代码已实施 | 纯数据层，无 Rml SDK 依赖 |
| **Phase 2** | Step 3 | ✅ 代码已实施 | RmlUIRenderer + 状态机（放在 NNRuntimeRmlui 模块） |
| **Phase 3** | Step 4 + 5 | ✅ 代码已实施 | ViewportRenderApi 编排 + API 连接 |
| **Phase 4** | Step 6 | ⚠️ 部分实施 | Native API 已定义，Editor 侧待编译验证 |

---

## 验证

**Phase 1 验证：**
- [ ] C++ 编译通过
- [ ] NNRmlUISystem 无 Rml SDK 依赖
- [ ] DrawList 正确构建（SortOrder 排序）
- [ ] 多实体引用同一 GUID → DrawList 中有多个条目

**Phase 2 验证：**
- [ ] RmlUIRenderer 独立管理文档生命周期
- [ ] RmlDocState 状态机正确转换
- [ ] Renderer 无 ECS 依赖

**Phase 3 验证：**
- [ ] SceneRenderer 正确调用 RmlUIRenderer
- [ ] 编辑模式 Scene View 显示 UI
- [ ] ViewMode 过滤正确

**Phase 4 验证：**
- [ ] 播放模式 UI 可交互
- [ ] 编辑模式 UI 不可交互
- [ ] AssetResolver 正确解析路径
