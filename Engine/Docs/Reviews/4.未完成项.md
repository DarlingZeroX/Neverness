未完成项（原因 + 后续方案）：
  - DropFile → AssetImporter：当前仅 Emit AssetCreated 事件。需要 AssetImporter API
  后补全实际导入逻辑
  - Viewport 同步：需要 ViewportPanel 集成后实现
  - ImGui DPI 缩放：需要访问 ImGuiIO.FontGlobalScale 后实现
  - waitEvent 阻塞等待：Phase 2 使用轮询简化实现，后续可用 condition_variable 改为真阻塞
