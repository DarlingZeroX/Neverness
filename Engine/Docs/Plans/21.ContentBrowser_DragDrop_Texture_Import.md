# Content Browser 拖拽导入纹理资产实施计划

**版本**: v1.0
**日期**: 2026-06-20
**状态**: 待审查

---

## 1. 目标

实现从外部（Windows 资源管理器）拖拽图片文件到 Content Browser 中，自动导入为纹理资产。

### 支持的图片格式
- `.png`, `.jpg`, `.jpeg`, `.tga`, `.bmp`, `.dds`, `.hdr`

### 用户体验流程
```
用户从资源管理器拖拽 player.png
    ↓
鼠标进入 Content Browser 区域，显示蓝色高亮边框
    ↓
松开鼠标，显示 Toast: "Importing player.png..."
    ↓
导入完成，显示 Toast: "✓ Imported player.png"
    ↓
Content Browser 自动刷新，显示新纹理资产
```

---

## 2. 架构设计

### 2.1 分层职责

```
┌─────────────────────────────────────────────────────────────┐
│  AvaloniaFrontend 层                                         │
│  ┌─────────────────────┐  ┌─────────────────────────────┐  │
│  │ ContentBrowser       │  │ AvaloniaDropHandler         │  │
│  │ ThumbnailGrid        │  │ (扩展：外部文件检测)         │  │
│  │ - DragEnter 高亮     │  │ - FilesDropped 事件         │  │
│  │ - DragOver 效果      │  │ - 文件路径提取              │  │
│  │ - Drop 触发导入      │  │                             │  │
│  └──────────┬──────────┘  └──────────────┬──────────────┘  │
│             │                            │                  │
│             └──────────┬─────────────────┘                  │
│                        ▼                                    │
│             ┌─────────────────────┐                         │
│             │ Toast 通知服务       │                         │
│             │ (简单实现)           │                         │
│             └──────────┬──────────┘                         │
└────────────────────────┼────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  Assets 层 (已有，无需修改)                                  │
│  ┌─────────────────────┐  ┌─────────────────────────────┐  │
│  │ ImageDropHandler     │  │ ImportPipeline              │  │
│  │ - 复制文件到 Assets/ │  │ - Meta 生成                 │  │
│  │ - 生成 .meta         │  │ - 编译 .nnasset             │  │
│  │ - 注册到数据库       │  │ - 注册到数据库              │  │
│  └─────────────────────┘  └─────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 关键决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 架构层级 | 扩展 AvaloniaDropHandler | 复用现有 stub，保持架构一致性 |
| 导入逻辑 | 调用 ImageDropHandler | 已有完整实现，避免重复代码 |
| 用户反馈 | V1: Toast 通知 | 实现成本低，体验接近 Unity |
| 拖拽目标 | 只支持当前目录 | 简化 V1 实现，V2 再支持子目录 |

---

## 3. 实施步骤

### Phase 1: 扩展 AvaloniaDropHandler (外部文件检测)

**目标**: 让 AvaloniaDropHandler 能够检测从外部拖入的文件

**修改文件**: `Engine/Source/Managed/Editor/Neverness.Editor.AvaloniaFrontend/DragDrop/AvaloniaDropHandler.cs`

```csharp
/// <summary>
/// Avalonia 拖放处理器——处理控件级别的拖放事件。
///
/// 支持两种拖拽：
/// 1. 内部资产拖拽（Content Browser → Inspector）
/// 2. 外部文件拖拽（Windows 资源管理器 → Content Browser）
/// </summary>
public class AvaloniaDropHandler
{
    private readonly List<Control> _attachedControls = new();

    /// <summary>外部文件拖入事件（文件系统路径数组）。</summary>
    public event Action<string[]>? FilesDropped;

    /// <summary>内部资产拖拽事件（GUID, TypeId）。</summary>
    public event Action<GUID, ulong>? AssetDropped;

    /// <summary>附加到控件，启用拖放支持。</summary>
    public void Attach(Control control)
    {
        if (_attachedControls.Contains(control))
            return;

        _attachedControls.Add(control);

        // 订阅 Avalonia DragDrop 事件
        Avalonia.Input.DragDrop.SetAllowDrop(control, true);
        Avalonia.Input.DragDrop.AddDragEnterHandler(control, OnDragEnter);
        Avalonia.Input.DragDrop.AddDragOverHandler(control, OnDragOver);
        Avalonia.Input.DragDrop.AddDragLeaveHandler(control, OnDragLeave);
        Avalonia.Input.DragDrop.AddDropHandler(control, OnDrop);
    }

    /// <summary>从控件分离，禁用拖放支持。</summary>
    public void Detach(Control control)
    {
        if (!_attachedControls.Contains(control))
            return;

        _attachedControls.Remove(control);

        // 取消订阅
        Avalonia.Input.DragDrop.SetAllowDrop(control, false);
        // 注意：Avalonia 目前没有 RemoveHandler API，需要依赖控件销毁
    }

    /// <summary>分离所有控件。</summary>
    public void DetachAll()
    {
        foreach (var control in _attachedControls.ToList())
        {
            Detach(control);
        }
    }

    // ── 事件处理 ──

    private void OnDragEnter(object? sender, DragEventArgs e)
    {
        // 检查是否包含文件
        if (HasFiles(e))
        {
            e.DragEffects = DragDropEffects.Copy;
            // 触发高亮效果（由调用方处理）
        }
        else
        {
            e.DragEffects = DragDropEffects.None;
        }
    }

    private void OnDragOver(object? sender, DragEventArgs e)
    {
        e.DragEffects = HasFiles(e) ? DragDropEffects.Copy : DragDropEffects.None;
    }

    private void OnDragLeave(object? sender, DragEventArgs e)
    {
        // 取消高亮效果（由调用方处理）
    }

    private void OnDrop(object? sender, DragEventArgs e)
    {
        if (!HasFiles(e))
            return;

        var files = GetFiles(e);
        if (files.Length > 0)
        {
            FilesDropped?.Invoke(files);
        }
    }

    // ── 辅助方法 ──

    /// <summary>检查拖拽数据是否包含文件。</summary>
    private static bool HasFiles(DragEventArgs e)
    {
        return e.Data.Contains(DataFormats.Files);
    }

    /// <summary>提取拖拽的文件路径列表。</summary>
    private static string[] GetFiles(DragEventArgs e)
    {
        var files = e.Data.GetFiles();
        if (files == null)
            return Array.Empty<string>();

        return files
            .Select(f => f.Path.LocalPath)
            .Where(p => !string.IsNullOrEmpty(p))
            .ToArray();
    }
}
```

### Phase 2: ContentBrowserThumbnailGrid 添加拖拽目标

**目标**: 在缩略图网格区域添加外部文件拖入支持

**修改文件**: `Engine/Source/Managed/Editor/Neverness.Editor.AvaloniaFrontend/Views/ContentBrowser/ContentBrowserThumbnailGrid.cs`

**新增字段**:
```csharp
// 拖拽导入相关
private AvaloniaDropHandler? _dropHandler;
private Border? _dropHighlight; // 拖拽高亮边框
```

**修改 Create() 方法**:
```csharp
internal Control Create()
{
    // ... 现有代码 ...

    // 添加拖拽高亮边框（初始隐藏）
    _dropHighlight = new Border
    {
        Background = Brushes.Transparent,
        BorderBrush = new SolidColorBrush(Color.FromArgb(0x80, 0x21, 0x96, 0xF3)),
        BorderThickness = new Thickness(3),
        CornerRadius = new CornerRadius(4),
        IsHitTestVisible = false,
        IsVisible = false,
        ZIndex = 100,
    };
    _selectionCanvas.Children.Add(_dropHighlight);

    // 初始化拖拽处理器
    _dropHandler = new AvaloniaDropHandler();
    _dropHandler.Attach(_fileScroll);
    _dropHandler.FilesDropped += OnFilesDropped;

    // ... 现有代码 ...
}
```

**新增方法**:
```csharp
/// <summary>处理外部文件拖入。</summary>
private void OnFilesDropped(string[] files)
{
    // 隐藏高亮
    if (_dropHighlight != null)
        _dropHighlight.IsVisible = false;

    // 过滤出支持的图片文件
    var imageExtensions = new HashSet<string>(
        new[] { ".png", ".jpg", ".jpeg", ".tga", ".bmp", ".dds", ".hdr" },
        StringComparer.OrdinalIgnoreCase);

    var imageFiles = files.Where(f => imageExtensions.Contains(Path.GetExtension(f))).ToArray();

    if (imageFiles.Length == 0)
        return;

    // 触发导入（通过 Controller）
    _controller.ImportDroppedFiles(imageFiles);
}
```

**修改 Dispose() 方法**:
```csharp
internal void Dispose()
{
    _dropHandler?.DetachAll();
    _dropHandler = null;

    _rubberBand?.Detach();
    _rubberBand = null;
}
```

### Phase 3: ContentBrowserController 添加导入方法

**目标**: 在 Controller 层添加文件导入逻辑

**修改文件**: `Engine/Source/Managed/Editor/Neverness.Editor.Core/Controllers/ContentBrowserController.cs`

**新增方法**:
```csharp
/// <summary>导入拖入的外部文件。</summary>
public void ImportDroppedFiles(string[] filePaths)
{
    // 获取当前目录路径
    var currentDir = _viewModel.CurrentDirectory;
    if (string.IsNullOrEmpty(currentDir))
        return;

    // 异步导入（避免阻塞 UI）
    Task.Run(() =>
    {
        var successCount = 0;
        var failCount = 0;

        foreach (var filePath in filePaths)
        {
            try
            {
                // 显示导入中 Toast
                ShowToast($"Importing {Path.GetFileName(filePath)}...", ToastType.Info);

                // 调用 ImageDropHandler
                var handler = new ImageDropHandler();
                if (handler.CanHandle(filePath))
                {
                    var assetsRoot = Path.Combine(currentDir);
                    if (handler.Handle(filePath, assetsRoot))
                    {
                        successCount++;
                        ShowToast($"✓ Imported {Path.GetFileName(filePath)}", ToastType.Success);
                    }
                    else
                    {
                        failCount++;
                        ShowToast($"✗ Failed to import {Path.GetFileName(filePath)}", ToastType.Error);
                    }
                }
            }
            catch (Exception ex)
            {
                failCount++;
                ShowToast($"✗ Error: {ex.Message}", ToastType.Error);
            }
        }

        // 批量导入结果
        if (filePaths.Length > 1)
        {
            if (failCount == 0)
                ShowToast($"✓ Imported {successCount} assets", ToastType.Success);
            else
                ShowToast($"✓ {successCount} succeeded, ✗ {failed} failed", ToastType.Warning);
        }

        // 刷新 Content Browser
        RefreshCurrentDirectory();
    });
}
```

### Phase 4: 实现简单 Toast 通知服务

**目标**: 实现 V1 版本的 Toast 通知

**新建文件**: `Engine/Source/Managed/Editor/Neverness.Editor.AvaloniaFrontend/Services/ToastService.cs`

```csharp
using Avalonia;
using Avalonia.Controls;
using Avalonia.Layout;
using Avalonia.Media;
using Avalonia.Threading;

namespace Neverness.Editor.AvaloniaFrontend.Services;

/// <summary>Toast 通知类型。</summary>
public enum ToastType
{
    Info,
    Success,
    Warning,
    Error
}

/// <summary>
/// 简单的 Toast 通知服务——V1 实现。
///
/// 显示在 Content Browser 右下角，2-3 秒后自动消失。
/// </summary>
public class ToastService
{
    private static ToastService? _instance;
    public static ToastService Instance => _instance ??= new ToastService();

    private readonly Queue<ToastMessage> _queue = new();
    private Panel? _container;
    private bool _isShowing;

    /// <summary>设置 Toast 容器（通常是主窗口的 Overlay 层）。</summary>
    public void SetContainer(Panel container)
    {
        _container = container;
    }

    /// <summary>显示 Toast 消息。</summary>
    public void Show(string message, ToastType type = ToastType.Info, int durationMs = 2500)
    {
        if (_container == null)
            return;

        var toast = new ToastMessage(message, type, durationMs);
        _queue.Enqueue(toast);

        if (!_isShowing)
        {
            ShowNext();
        }
    }

    private void ShowNext()
    {
        if (_queue.Count == 0 || _container == null)
        {
            _isShowing = false;
            return;
        }

        _isShowing = true;
        var toast = _queue.Dequeue();

        // 创建 Toast UI
        var border = CreateToastBorder(toast);

        // 添加到容器
        _container.Children.Add(border);

        // 定位到右下角
        Canvas.SetRight(border, 16);
        Canvas.SetBottom(border, 16);

        // 自动消失
        Task.Delay(toast.DurationMs).ContinueWith(_ =>
        {
            Dispatcher.UIThread.Post(() =>
            {
                _container.Children.Remove(border);
                ShowNext();
            });
        });
    }

    private Border CreateToastBorder(ToastMessage toast)
    {
        var (bgColor, iconColor, icon) = toast.Type switch
        {
            ToastType.Success => ("#FF2D7D2D", "#FF4CAF50", "✓"),
            ToastType.Warning => ("#FFFF9800", "#FFFF9800", "⚠"),
            ToastType.Error => ("#FFD32F2F", "#FFF44336", "✗"),
            _ => ("#FF1976D2", "#FF2196F3", "ℹ"),
        };

        return new Border
        {
            Background = new SolidColorBrush(Color.Parse(bgColor)),
            CornerRadius = new CornerRadius(4),
            Padding = new Thickness(12, 8),
            MaxWidth = 300,
            BoxShadow = new BoxShadows(new BoxShadow
            {
                Color = Color.FromArgb(0x40, 0x00, 0x00, 0x00),
                Blur = 8,
                OffsetX = 0,
                OffsetY = 2,
            }),
            Child = new StackPanel
            {
                Orientation = Orientation.Horizontal,
                Spacing = 8,
                Children =
                {
                    new TextBlock
                    {
                        Text = icon,
                        FontSize = 14,
                        Foreground = new SolidColorBrush(Color.Parse(iconColor)),
                        VerticalAlignment = VerticalAlignment.Center,
                    },
                    new TextBlock
                    {
                        Text = toast.Message,
                        FontSize = 12,
                        Foreground = new SolidColorBrush(Color.Parse("#FFFFFFFF")),
                        TextTrimming = TextTrimming.CharacterEllipsis,
                        MaxLines = 2,
                        VerticalAlignment = VerticalAlignment.Center,
                    }
                }
            }
        };
    }

    private record ToastMessage(string Message, ToastType Type, int DurationMs);
}
```

### Phase 5: 集成 Toast 到 ContentBrowserController

**目标**: 在导入过程中显示 Toast 通知

**修改文件**: `Engine/Source/Managed/Editor/Neverness.Editor.Core/Controllers/ContentBrowserController.cs`

```csharp
// 在 ImportDroppedFiles 方法中使用 Toast
private void ShowToast(string message, ToastType type)
{
    // 通过事件总线发送 Toast 请求
    _eventBus.Publish(new EditorEvent(EditorEventType.ShowToast, new ToastRequest(message, type)));
}
```

**修改文件**: `Engine/Source/Managed/Editor/Neverness.Editor.Core/Public/IEditorEventBus.cs`

```csharp
// 在 EditorEventType 枚举中添加
ShowToast,
```

### Phase 6: 处理拖拽高亮效果

**目标**: 拖拽进入时显示蓝色高亮边框

**修改文件**: `Engine/Source/Managed/Editor/Neverness.Editor.AvaloniaFrontend/Views/ContentBrowser/ContentBrowserThumbnailGrid.cs`

```csharp
// 在 Create() 方法中订阅 AvaloniaDropHandler 事件
_dropHandler.DragEnter += () =>
{
    if (_dropHighlight != null)
        _dropHighlight.IsVisible = true;
};

_dropHandler.DragLeave += () =>
{
    if (_dropHighlight != null)
        _dropHighlight.IsVisible = false;
};
```

**修改 AvaloniaDropHandler.cs**:
```csharp
// 添加事件
public event Action? DragEnter;
public event Action? DragLeave;

private void OnDragEnter(object? sender, DragEventArgs e)
{
    if (HasFiles(e))
    {
        e.DragEffects = DragDropEffects.Copy;
        DragEnter?.Invoke();
    }
    else
    {
        e.DragEffects = DragDropEffects.None;
    }
}

private void OnDragLeave(object? sender, DragEventArgs e)
{
    DragLeave?.Invoke();
}
```

---

## 4. 文件清单

### 新建文件 (2 个)
| 文件 | 说明 |
|------|------|
| `Engine/Source/Managed/Editor/Neverness.Editor.AvaloniaFrontend/Services/ToastService.cs` | Toast 通知服务 |
| `Engine/Source/Managed/Editor/Neverness.Editor.Core/Public/ToastRequest.cs` | Toast 请求数据结构 |

### 修改文件 (6 个)
| 文件 | 修改内容 |
|------|----------|
| `Engine/Source/Managed/Editor/Neverness.Editor.AvaloniaFrontend/DragDrop/AvaloniaDropHandler.cs` | 实现外部文件拖入检测 |
| `Engine/Source/Managed/Editor/Neverness.Editor.AvaloniaFrontend/Views/ContentBrowser/ContentBrowserThumbnailGrid.cs` | 添加拖拽目标区域和高亮效果 |
| `Engine/Source/Managed/Editor/Neverness.Editor.AvaloniaFrontend/Views/MainEditorWindow.axaml` | 添加 ToastOverlay Canvas |
| `Engine/Source/Managed/Editor/Neverness.Editor.AvaloniaFrontend/Views/MainEditorWindow.axaml.cs` | 初始化 ToastService |
| `Engine/Source/Managed/Editor/Neverness.Editor.AvaloniaFrontend/Public/AvaloniaFrontendModule.cs` | 订阅 ShowToast 事件 |
| `Engine/Source/Managed/Editor/Neverness.Editor.Core/Controllers/ContentBrowserController.cs` | 添加 ImportDroppedFiles 方法，使用事件总线发送 Toast |
| `Engine/Source/Managed/Editor/Neverness.Editor.Core/Public/IEditorEventBus.cs` | 添加 ShowToast 事件类型 |

### 已有文件 (无需修改)
| 文件 | 说明 |
|------|------|
| `Engine/Source/Managed/Editor/Neverness.Editor.Assets/Import/Importers/ImageDropHandler.cs` | 已有完整实现 |
| `Engine/Source/Managed/Editor/Neverness.Editor.Assets/Import/ImportPipeline.cs` | 已有完整实现 |

---

## 5. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Avalonia DragDrop API 不稳定 | 中 | 参考 AvaloniaInspectorBase 的实现，已验证可用 |
| 大文件导入阻塞 UI | 高 | 使用 Task.Run 异步处理，V1 先简单实现，V2 再优化 |
| 文件路径编码问题 | 低 | 使用 Path.GetFullPath 规范化路径 |
| 重复导入同一文件 | 低 | ImageDropHandler 已有 overwrite: true 处理 |

---

## 6. 测试计划

### 单元测试
- [ ] AvaloniaDropHandler 正确检测文件拖入
- [ ] 过滤出支持的图片格式
- [ ] Toast 消息正确显示和消失

### 集成测试
- [ ] 从资源管理器拖拽单个 .png 文件到 Content Browser
- [ ] 从资源管理器拖拽多个图片文件到 Content Browser
- [ ] 拖拽不支持的文件类型（如 .txt）不触发导入
- [ ] 导入后 Content Browser 自动刷新显示新资产
- [ ] Toast 通知正确显示导入状态

### 边界测试
- [ ] 拖拽到空白区域（非文件项）也能触发导入
- [ ] 拖拽过程中鼠标离开 Content Browser 区域
- [ ] 快速连续拖拽多个文件

---

## 7. 后续优化 (V2)

1. **支持子目录拖拽**: 可以拖拽到左侧目录树的特定子目录
2. **进度条**: 批量导入时显示进度条
3. **可取消导入**: 支持取消长时间运行的导入任务
4. **导入队列**: 后台任务队列，避免同时导入过多文件
5. **错误日志**: 点击 Toast 可查看详细错误日志

---

## 8. 实施时间估算

| Phase | 任务 | 估算时间 | 实际状态 |
|-------|------|----------|----------|
| Phase 1 | 扩展 AvaloniaDropHandler | 2 小时 | ✅ 完成 |
| Phase 2 | ContentBrowserThumbnailGrid 添加拖拽目标 | 2 小时 | ✅ 完成 |
| Phase 3 | ContentBrowserController 添加导入方法 | 1.5 小时 | ✅ 完成 |
| Phase 4 | 实现 Toast 通知服务 | 2 小时 | ✅ 完成 |
| Phase 5 | 集成 Toast 到 Controller | 1 小时 | ✅ 完成 |
| Phase 6 | 处理拖拽高亮效果 | 1 小时 | ✅ 完成 |
| Phase 7 | Avalonia 12.0 API 适配 | 1 小时 | ✅ 完成 |
| **总计** | | **10.5 小时** | **已完成** |

### 额外工作（不在原计划中）
- 事件总线集成：添加 ShowToast 事件类型和 ToastRequest 数据结构
- MainEditorWindow AXAML 修改：添加 ToastOverlay Canvas
- AvaloniaFrontendModule 事件订阅：订阅 ShowToast 事件并路由到 ToastService
- Avalonia 12.0 API 适配：使用 DataFormat.File 和 IDataTransfer 接口
- IDropImportService 接口：解耦 Core 和 Assets 层

---

## 9. Avalonia 12.0 API 适配

### 关键发现

Avalonia 12.0 的 DragDrop API 与 Avalonia 11 有显著差异：

| 特性 | Avalonia 11 | Avalonia 12.0 |
|------|-------------|---------------|
| 数据接口 | `IDataObject` | `IDataTransfer` |
| 文件格式 | `DataFormats.Files` | `DataFormat.File` |
| 获取文件 | `e.Data.GetFiles()` | `item.TryGetRaw(DataFormat.File)` |
| 文件类型 | `IEnumerable<IStorageItem>` | `IStorageItem` |

### 正确的 API 用法

```csharp
// 检查是否包含文件
private static bool HasFiles(DragEventArgs e)
{
    var data = e.DataTransfer;
    if (data == null) return false;
    return data.Formats.Contains(DataFormat.File);
}

// 获取文件路径
private static string[] GetFiles(DragEventArgs e)
{
    var data = e.DataTransfer;
    if (data == null) return Array.Empty<string>();

    var result = new List<string>();
    foreach (var item in data.Items)
    {
        if (!item.Formats.Contains(DataFormat.File))
            continue;

        var storageItem = item.TryGetRaw(DataFormat.File) as IStorageItem;
        if (storageItem == null) continue;

        var path = storageItem.TryGetLocalPath();
        if (!string.IsNullOrEmpty(path))
            result.Add(path);
    }
    return result.ToArray();
}
```

## 10. 审查要点

请重点审查以下问题：

1. **架构分层**: 是否应该将导入逻辑放在 Assets 层而不是 Controller？
2. **Toast 实现**: 当前的简单实现是否足够，还是需要更复杂的队列管理？
3. **异步处理**: Task.Run 的使用是否合适，还是需要更细粒度的线程控制？
4. **错误处理**: 导入失败时的错误恢复策略是否完善？
5. **API 兼容性**: Avalonia 12.0 的 DataFormat.File API 是否正确使用？

---

**计划完成日期**: 2026-06-20
**计划制定者**: MiMo
**审查状态**: 已批准
**实施状态**: 已完成
